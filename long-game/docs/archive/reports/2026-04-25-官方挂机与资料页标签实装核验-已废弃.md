---
tags:
  - long-game
  - archive
---

> **⚠️ 本文档已废弃**（2026-04-30 核查：与当前代码不符）
>
> AutoClickService 已移除，前端挂机已停用，资料页布局已变更

# 官方挂机与资料页标签实装核验

## 本次目标

- 删除战斗主页里的挂机控制面板。
- 把资料页原本固定在上方的账号内容并入顶部标签页。
- 将官方挂机控制放入资料页顶部标签页。
- 核验此前官方挂机改造是否已实装。

## 实装结论

- 后端已实装官方挂机托管：
  - `backend/internal/httpapi/auto_click_service.go` 维护内存任务表。
  - `backend/cmd/server/main.go` 创建 `AutoClickService`，使用 `time.Second / 3` 间隔，并设置 `AutoStart: true`。
  - `backend/internal/httpapi/auto_click_routes.go` 注册 `GET /api/auto-click`、`POST /api/auto-click/start`、`POST /api/auto-click/stop`。
- 前端已接入官方挂机接口：
  - `frontend/src/pages/publicPageState.js` 查询、开始、停止挂机均调用 `/api/auto-click*`。
  - 本次将挂机控制从战斗页移到资料页“挂机”标签。
  - 本次确认前端没有恢复旧的本地 `setTimeout` 挂机循环。

## 页面调整

- 战斗页：
  - 移除“官方挂机托管”面板。
  - 保留 Boss 分区、掉落池、Boss 伤害榜和实时排行榜。
- 资料页：
  - 顶部标签调整为“账号 / 挂机 / 背包 / 属性 / 装备栏”。
  - 登录、切换账号、退出登录进入“账号”标签。
  - 官方挂机状态、目标、频率和启停按钮进入“挂机”标签。

## 回归范围

- `frontend/src/pages/PublicPage.threePageRefactor.test.js`
  - 新增断言：战斗页不再包含挂机面板。
  - 新增断言：资料页顶部标签承载账号与官方挂机。
  - 保留断言：官方挂机仍调用服务端 `/api/auto-click*`。

