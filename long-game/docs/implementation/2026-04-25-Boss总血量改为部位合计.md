---
tags:
  - long-game
  - implementation
---

# Boss 总血量改为部位合计

## 目标

后台 Boss 模板不再手动填写总血量。

## 实装

- Boss 模板表单中的总血量改为只读展示。
- 展示值由当前部位布局里的 `maxHp` 合计得出。
- 保存 Boss 模板时，前端提交的 `maxHp` 同样来自部位血量合计。
- 后端已在保存模板、直接激活和模板池激活时按部位血量归一，保证实例总血量与部位总血量一致。

